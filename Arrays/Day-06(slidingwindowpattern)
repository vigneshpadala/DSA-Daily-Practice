#SLIDIND WINDOW PATTERN
#TYPES OF VARIABLE SIZE SLIDING WINDOW
#BASIC PROBLEM METHOD
left = 0
window_sum = 0
min_len = infinity

for right in range(n):
    window_sum += arr[right]

    while condition_valid:
        min_len = min(min_len, right - left + 1)
        window_sum -= arr[left]
        left += 1
